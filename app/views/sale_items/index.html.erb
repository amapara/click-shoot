<h1>Sale Item Index</h1>
<!--  t.string "model"
    t.float "daily_rate"
    t.float "set_up_rate"
    t.bigint "user_id"
    t.date "start_date"
    t.date "end_date" -->
<% @sale_items.each do |item| %>
  <h2><%= item.id %> - <%= item.model %></h2>

<% end %>
